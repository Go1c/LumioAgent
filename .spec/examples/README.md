# Examples（走查样板）

本目录存放 LumioAgent 工作流的走查样板和示例。

示例只用于说明流程，不定义新规则。如果示例暴露出规则缺口，必须把规则补到正确的权威源：

- 全局策略：`.spec/AGENTS.md`
- 共享规则片段：`.spec/rules/*.md`
- 角色行为：`.spec/agents/*.agent.md`
- 可复用方法：`.spec/skills/<name>/SKILL.md`
